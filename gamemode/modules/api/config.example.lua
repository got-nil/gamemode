return {
    realm = "server",
    config = {
        ws_host = "ws://127.0.0.1:8000/ws",
        ws_token = "xxxxxxxxxxxxxxxxxxxx",
        ws_verify_cert = false,

        ip_whitelist = {
            "127.0.0.1"
        },

        debug_logs = false
    }
}